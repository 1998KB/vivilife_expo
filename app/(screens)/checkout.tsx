import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const checkout = () => {
  return (
    <SafeAreaView>
      <Text>checkout</Text>
    </SafeAreaView>
  );
};

export default checkout;
